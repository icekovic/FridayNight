@page
@model FridayNight.Pages.Places.IndexModel
@{
}

<h1 class="text-center">Places to go</h1>

<form method="post">
    <table class="table thead-dark">
        <thread>
            <tr>
                <th>Categories</th>
                <th>Places</th>
            </tr>
        </thread>
        <tbody>
            @*@foreach (var category in Model.CategoriesSelectList)
        {
            <tr>
                <td>@category.Text</td>
            </tr>
        }*@
            @foreach (var category in Model.Categories)
            {
                <tr>
                    <td>
                        @*<select asp-for="Category.Idcategory" class="form-control"
                            asp-items="@Model.CategoriesSelectList" name="CategoriesSelectList"></select>*@
                        <button asp-page="Places/Index">@category.Name</button>
                        <input type="hidden" asp-for="@category.Idcategory"/>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</form>